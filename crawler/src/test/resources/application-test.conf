jdbc = {
  driver = "slick.jdbc.H2Profile$"
  db {
    url = "jdbc:h2:mem:test;INIT=create schema if not exists test;DB_CLOSE_DELAY=-1;DATABASE_TO_UPPER=false;"
    driver = "org.h2.Driver"
    connectionPool = disabled
    keepAliveConnection = true
    user = sa
    password = ""
  }
}